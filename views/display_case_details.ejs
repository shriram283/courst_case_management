<!DOCTYPE html>
<html lang="en">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/css/register_profile_style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #333;
            text-align: center; /* Center align the heading */
        }

        label {
            font-weight: normal; /* Ensure labels are not bold */
        }

        .back-button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 15px;
            display: inline-block;
            text-decoration: none;
        }

        .back-button:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <h2><i class="fa fa-user-plus" style="font-size:32px"></i> <%= case_name %>'s Details</h2>

    <div class="container">
        <form action="<%= user_type === 'c' ? '/client/dashboard' : '/lawyer/dashboard' %>" method="GET">
            <label>Case Name:</label>
            <span><%= case_name %></span><br><br>

            <label>Case Type:</label>
            <span><%= case_type %></span><br><br>

            <label>Case Description:</label>
            <span><%= case_descp %></span><br><br>

            <button type="submit" class="back-button">Back</button>
        </form>

        <% if (user_type === "c") { %>
            <form action="/lawyer/display_lawyer_profile/<%= lawyer_id %>" method="GET" id="lawyer-details-form"></form>
            <script src="/js/show_lawyer_details_btn.js"></script>
        <% } %>
    </div>
</body>

</html>
